version: 0.2

phases:
  pre_build:
    commands:
      - chmod +x ./gradlew
  build:
    commands:
      - ./gradlew clean bootJar


artifacts:
  files:
    - appspec.yml
    - scripts/**/*
    - build/libs/*.jar
  discard-paths: no